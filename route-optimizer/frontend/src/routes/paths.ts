/** Client path for the main Route Optimizer UI. */
export const ROUTE_OPTIMIZER_PATH = "/routeoptimizer";

export const PROFILE_PATH = "/profile";
export const FEEDBACK_PATH = "/feedback";
export const ROUTE_HISTORY_PATH = "/route-history";

/** Supabase password recovery email must redirect here (add to Supabase Auth URL config). */
export const RESET_PASSWORD_PATH = "/reset-password";
